spring:
  application:
    name: realtime-service
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
