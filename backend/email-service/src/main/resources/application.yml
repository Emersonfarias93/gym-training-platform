spring:
  application:
    name: email-service
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
